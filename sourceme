DIR=$(cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
VENV=.venv

if [[ ! -e $VENV ]]; then
    uv venv $VENV
    source $VENV/bin/activate
    uv pip install -e ".[dev]"
else
    source $VENV/bin/activate
fi

pre-commit install
